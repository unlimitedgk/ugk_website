// -----------------------------
// 2. Camp type (types/camp.ts)
// -----------------------------
export type Camp = {
  id: string
  title: string
  start_date: string
  end_date: string
  location: string
  description: string
}
